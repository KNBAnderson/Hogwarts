import {PotterInfo} from '../src/potterApi';

/* eslint-disable no-undef */
describe('Character', () => {
  let info = new Character;
  let house = info.getHouse();

  // describe('Chatacter Constructor', () => {
  //     test('create instance of Character', () => {
  //         expect(typeof info).toBe("object");
  //     })
  // })
  
  // describe('Character Method', () => {
  //     test('assign a house', () => {
  //       console.log("here", info.getHouse());
  //         expect(typeof info.getHouse()).toBe("string");
  //     })
  // })
  
})
